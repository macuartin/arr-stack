#!/usr/bin/env bash
# Hook pre-commit: bloquea el commit si hay secretos en configs/ o en lo staged.
# Activar con: git config core.hooksPath scripts/githooks
exec "$(git rev-parse --show-toplevel)/scripts/config-check.sh"
