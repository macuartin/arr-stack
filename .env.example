# Copia este archivo a `.env` y ajusta los valores a tu host.
#   cp .env.example .env

# UID/GID del usuario propietario de los archivos (id -u / id -g)
PUID=1000
PGID=1000

# Zona horaria (https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=Europe/Madrid
